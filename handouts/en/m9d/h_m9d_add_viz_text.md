---
marp: true
theme: fastr-handout
paginate: true
footer: "FASTR · Slide Decks"
---

<div class="brand-line"><span class="rule"></span><img src="../../../resources/logos/FASTR_Primary_01_FullName.png" alt="FASTR" height="28"></div>

# Add a visualisation + interpretation text

<p class="meta-line"><strong>Reference</strong> · <strong>Slide Decks</strong> · <strong>~5 min</strong></p>

## What this handout is

A short reference for the **layout of a content slide** — how to add a chart on one side and interpretation text on the other. Use this with the next two activities (adding a visualisation manually, or via the AI Assistant).

## The three building blocks

### 1. Add a visualisation

In the slide editor, choose **Block** → **Visualisation** → **Select visualisation** → browse your saved charts → click **Select**.

The chart appears on the slide. By default it takes the full slide width.

### 2. Add text next to it

Right-click in the slide preview → **Add → Col to right** → click the new text area and start typing.

> **Use `#` for headings.** A short headline at the top of the text column anchors what the chart is showing. One sentence below it is the takeaway.

<div class="screenshot-placeholder">
📷 <strong>Right-click menu showing "Add → Col to right"</strong><br>
<code>resources/screenshots/m9d/col_to_right_menu.png</code>
</div>

### 3. Edit slide details and save

Switch to the **Slide** tab (in the editor panel) → edit the slide title, subtitle, and date → click **Save**.

## Layout tips

> **Two columns is the default for a reason.** Chart on the left, takeaway on the right, headline at the top. Don't try to fit four columns on one slide — split into two slides if the content demands it.

- **More options:** Right-click → **Add → Row below** — useful for footnotes or a source line at the bottom.
- **Resize anytime:** the vertical divider between chart and text is draggable (see *Editor walkthrough* facilitator handout).

## What's next

Use this reference while doing **Add a visualisation (manually)** or **Add a visualisation (with the AI Assistant)** — the next two activities.

> 🔎 **Verify in your current UI**: menu paths and naming may differ slightly. The three-step layout (visualisation → text column → save) is unchanged.
