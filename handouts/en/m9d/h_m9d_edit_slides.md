---
marp: true
theme: fastr-handout
paginate: true
footer: "FASTR · Slide Decks"
---

<div class="brand-line"><span class="rule"></span><img src="../../../resources/logos/FASTR_Primary_01_FullName.png" alt="FASTR" height="28"></div>

<div class="setup-breadcrumb"><span class="step done">Create deck</span> <span class="arrow">→</span> <span class="step done">Layout reference</span> <span class="arrow">→</span> <span class="step done">Add manually</span> <span class="arrow">→</span> <span class="step done">Add with AI</span> <span class="arrow">→</span> <span class="step current">Edit & finalise</span></div>

# Edit and finalise your slides

<p class="meta-line"><strong>Activity</strong> · <strong>Slide Decks</strong> · <strong>~20 min</strong></p>

## Before you start

- ☐ Your slide deck has at least two slides with visualisations and interpretation text
- ☐ The deck is saved
- ☐ The facilitator has run through the *Editor walkthrough* demo (so you know where the resize divider and the Header/Content tabs are)

## Why this matters

The AI generates the content, but the **layout often needs adjusting**: text can appear squeezed, a chart may need more room, the headline may not match the data. A two-minute review per slide is the difference between something usable and something polished.

## The four passes

Work through each slide in your deck and make these four passes:

### 1. Review each slide for accuracy

Read each slide as if you'd never seen the analysis. Does the headline match what the chart shows? Is the interpretation text correct? Mark any slide that needs an AI rewrite.

### 2. Resize text boxes

Make sure interpretation text is fully visible. Drag the vertical divider between the chart and the text to give text more room if it's running off the edge.

### 3. Resize and reposition visualisations

Ensure each chart is **clear**, **well-balanced**, and properly aligned. If a chart looks small, drag the divider the other way (more chart, less text).

### 4. Adjust layout and spacing

Fine-tune the overall slide: line breaks, alignment, the date and footer. Stop when each slide could be in front of a Director without you cringing.

---

<div class="brand-line"><span class="rule"></span><img src="../../../resources/logos/FASTR_Primary_01_FullName.png" alt="FASTR" height="28"></div>

## When to ask the AI to rewrite

Sometimes the interpretation text just isn't right. Rather than rewriting it yourself, go back to the AI conversation and ask:

> Rewrite the interpretation for slide [X] to focus on [the angle you want] and shorten it to one paragraph.

The AI will update the slide.

## Checkpoint

Each slide in your deck should now:

- Have a clear headline that matches the chart
- Show the chart at a readable size, properly aligned
- Have interpretation text that's fully visible and well-edited
- Have the slide title, subtitle, and date filled in

## What could go wrong

- **Resize divider doesn't appear** — click into the slide first to enter the editor view. The divider only shows when a content slide is open.
- **Chart still looks cramped after resizing** — the chart itself may be too detailed for slide format. Open it in the **Visualisations** tab, simplify (fewer series, less text), save, and re-add to the slide.
- **You lose changes** — the editor saves automatically, but the **Save** button at the top of the deck is the safety net. Use it after major edits.

> 🔎 **Verify in your current UI**: editor panel layout and tab names (*Slide*, *Content*, *Header/Footer*) may differ. The four passes apply regardless.

## What's next

Your deck is ready to share. The next module (or next session) covers exporting and dissemination.
