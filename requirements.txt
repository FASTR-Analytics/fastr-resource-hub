# FASTR Resource Hub - Python Dependencies
# Install: pip3 install -r requirements.txt
#
# These are the third-party packages the scripts in tools/ need.

# Core — used by tools/00_extract_slides.py, validate_content.py,
# migrate_to_meta.py, generate_catalog.py, translate.py, translate_docs.py,
# build_handout_pdfs.py, etc.
PyYAML>=6.0

# Translation — used by tools/translate.py + translate_docs.py (DeepL API).
requests>=2.28.0

# Diagram rendering — used by tools/build_fastr_diagrams.py to rasterise
# brand SVGs to PNG for slide backgrounds.
cairosvg>=2.7.0

# Booklet PDF assembly — used by tools/build_booklet.py to stitch per-handout
# PDFs into a printable booklet with a cover and pagination.
pypdf>=4.0.0
reportlab>=4.0.0
