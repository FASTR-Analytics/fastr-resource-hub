# Server
PORT=3001
NODE_ENV=development

# Anthropic API (required for AI features)
ANTHROPIC_API_KEY=your_api_key_here

# Database (optional - defaults to ./data/workshops.db)
DATABASE_PATH=./data/workshops.db
